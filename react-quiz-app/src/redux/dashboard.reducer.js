const initialState = {
    category: null,
    difficulty: null,
    type: null,
    amount: null
}
export const dasdboardRuducer = (state = initialState, {type, payload}) => {
    switch(type){
        default:
            return state 
    }
}